import React, { FC } from 'react';

interface IProps {
  [x: string]: any;
}

const BlockPage: FC<IProps> = (props) => {
  const {  } = props;
  return (
    <div>

    </div>
  );
};

BlockPage.defaultProps = {};

export default BlockPage;
